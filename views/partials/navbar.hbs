<nav class="navbar navbar-dark navbar-navy px-3 py-2 d-flex align-items-center justify-content-between" style="height:56px;">
  <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/dashboard">
    <i class="fas fa-server text-primary"></i>
    <span>IT Inventory</span>
  </a>
  <div class="d-flex align-items-center gap-3">
    {{#if currentUser}}
      <span class="text-white-50 small">
        <i class="fas fa-user me-1"></i>{{currentUser.username}}
        <span class="badge ms-1 bg-{{roleBadge currentUser.role}}">{{currentUser.role}}</span>
      </span>
      <a href="/logout" class="btn btn-sm btn-outline-danger">
        <i class="fas fa-sign-out-alt me-1"></i>Logout
      </a>
    {{/if}}
  </div>
</nav>
